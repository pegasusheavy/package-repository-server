[advisories]
ignore = [
    # RUSTSEC-2023-0071 (rsa: Marvin timing sidechannel) — no fixed release
    # exists upstream. Pulled in via openidconnect -> rsa; only used for
    # JWT signature verification of IdP tokens, not RSA decryption with
    # attacker-observable timing. Revisit when rsa publishes a fix.
    "RUSTSEC-2023-0071",
]
